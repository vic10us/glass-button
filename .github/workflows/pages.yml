name: Deploy demo to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm test
      - run: npm run build
      - name: Assemble site
        # The demo lives at demo/index.html and loads ../dist/. Publish it as
        # the site root with dist/ alongside so the URL is just the origin.
        run: |
          mkdir -p site/dist
          cp dist/* site/dist/
          sed 's#\.\./dist/#./dist/#g' demo/index.html > site/index.html
          touch site/.nojekyll
      - uses: actions/configure-pages@v5
        with:
          # Enable Pages (source: GitHub Actions) on first run if it is not yet enabled.
          enablement: true
      - uses: actions/upload-pages-artifact@v3
        with:
          path: site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
